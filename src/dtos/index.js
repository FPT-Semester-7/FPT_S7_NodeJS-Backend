const BookingDTO = require('./BookingDTO');
const MCProfileDTO = require('./MCProfileDTO');
const ResourceDTO = require('./ResourceDTO');
const ScriptDTO = require('./ScriptDTO');
const TransactionDTO = require('./TransactionDTO');
const UserDTO = require('./UserDTO');

module.exports = {
    BookingDTO,
    MCProfileDTO,
    ResourceDTO,
    ScriptDTO,
    TransactionDTO,
    UserDTO
};
